## Agent skills

### Issue tracker

问题与规格使用 `nixihz/capslock-bye` 的 GitHub Issues，通过 `gh` CLI 操作。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认 triage 标签，各角色与实际标签同名。参见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context：根目录 `CONTEXT.md` 与 `docs/adr/`。参见 `docs/agents/domain.md`。
